# 📚 [React] 상태 관리 라이브러리 비교 및 렌더링 최적화 원리

## 🎯 학습 목표
- **학습 동기**: 프로젝트 진행을 위해 프로젝트 성격(규모, 데이터 복잡도)에 맞는 최적의 상태 관리 기술을 선정하고, 불필요한 리렌더링을 방지하는 원리를 이해하기 위함.
- **학습 후 기대 결과**: 
  1. Context API, Redux, Zustand, Recoil, Jotai의 장단점을 명확히 구분하여 상황에 맞게 선택할 수 있다.
  2. Flux 패턴, Atomic 패턴, Observer 패턴의 작동 원리를 설명할 수 있다.
  3. 자바스크립트 참조값(Reference) 특성을 이해하고, Memoization을 통해 렌더링 성능을 최적화할 수 있다.

## 📖 핵심 내용 요약
- **핵심 개념 1**: 상태 관리 라이브러리 비교 (Context API vs Zustand vs Redux vs Atomic)
- **핵심 개념 2**: 주요 소프트웨어 디자인 패턴 (Flux, Atomic, Observer/Pub-Sub)
- **핵심 개념 3**: 렌더링 최적화와 Memoization (Selector 패턴과 참조값 비교)

## ✍️ 상세 정리

### 개념 1: React 상태 관리 라이브러리 비교 분석

각 라이브러리는 **성능(렌더링 제어)**과 **유지보수(개발 편의성)** 관점에서 뚜렷한 차이를 보임.

| 라이브러리 | 패턴 | 성능 (렌더링 제어) | 유지보수 및 특징 | 추천 상황 |
| :--- | :--- | :--- | :--- | :--- |
| **Context API** | 의존성 주입 | ⚠️ **주의**: Provider 값 변경 시 하위 컴포넌트 전체 리렌더링 위험 (수동 최적화 필요) | 설정이 간편하나, 규모가 커지면 Provider가 중첩되는 Wrapper Hell 발생 가능 | 테마, 언어 설정 등 변경이 적은 단순 전역 상수 |
| **Zustand** | Flux (간소화) | 🏆 **탁월**: Selector를 통해 필요한 데이터만 구독, 변경 시 해당 컴포넌트만 리렌더링 | 보일러플레이트가 매우 적고 코드가 직관적임. (단, 팀 컨벤션 필요) | 대부분의 모던 React 프로젝트 (특히 주니어 개발자 추천) |
| **Redux (RTK)** | Flux (표준) | 👍 **우수**: `useSelector`로 세밀한 구독 가능 | **디버깅(DevTools)이 매우 강력**하나, 초기 설정과 코드가 많음(Boilerplate) | 대규모 엔터프라이즈, 상태 변경 히스토리 추적이 중요한 경우 |
| **Jotai / Recoil** | Atomic | ✨ **특화**: 데이터 간 의존성이 복잡해도 필요한 부분만 딱 골라 렌더링 | Bottom-up 방식. 데이터 파생 로직(Derived State) 관리에 유리 | 엑셀, 대시보드, 그래픽 에디터 등 복잡한 데이터 연산이 많은 앱 |

### 개념 2: 핵심 아키텍처 및 디자인 패턴

각 라이브러리가 근간으로 삼고 있는 작동 원리 및 철학.

#### 1. Flux 아키텍처 (Redux, Zustand)
> **"데이터는 한 방향으로만 흐른다 (Unidirectional Data Flow)"**
- **흐름**: Action → Dispatcher → Store → View
- **특징**: 데이터 변경 경로가 하나뿐이라 흐름을 예측하기 쉽고 버그 추적이 용이함.

#### 2. Atomic 패턴 (Recoil, Jotai)
> **"작은 원자들을 조립한다 (Bottom-up)"**
- **특징**: 상태의 최소 단위인 **Atom**을 정의하고, 컴포넌트가 이를 레고 블록처럼 조립해 사용.
- **장점**: 상태 그래프(Dependency Graph)를 형성하여 데이터 간의 의존성 관리에 최적화됨.

#### 3. Observer / Pub-Sub 패턴
> **"변화가 생기면 구독자에게 알려준다"**
- **원리**: Zustand 등이 사용하는 방식. 상태(Subject)가 변하면 이를 **구독(Subscribe)**하고 있는 컴포넌트(Observer)에게만 알림을 보내 리렌더링을 유발함.
- **비유**: 유튜브 구독 알림 (영상이 올라오면 구독자에게만 알림이 감).

### 개념 3: 렌더링 최적화와 Memoization

#### 1. Context API vs Zustand의 렌더링 메커니즘 차이
- **Context API**: 데이터가 변하면 연결된 모든 컴포넌트에 **"방송(Broadcast)"**을 함. (불필요한 렌더링 발생)
- **Zustand**: **Selector**를 사용해 내가 필요한 데이터가 변했는지 비교하고, 변했을 때만 **"연락"**을 받음.

#### 2. Memoization (메모이제이션)이 필요한 이유
자바스크립트에서 객체(Object, Array)는 **참조값(메모리 주소)**으로 비교된다. 내용은 같아도 새로 생성하면 "다른 객체"로 인식하여 리렌더링을 유발한다.

```javascript
// [Memoization을 안 했을 때의 문제점]
// 컴포넌트가 리렌더링 될 때마다 value 객체가 새로 생성됨 (주소 변경)
// -> Context를 구독하는 모든 하위 컴포넌트가 "값이 변했다"고 판단하여 불필요하게 리렌더링 됨.
const MyProvider = () => {
  const value = { name: "User", age: 25 }; 
  return <Context.Provider value={value}>...</Context.Provider>;
}

// [useMemo로 해결]
// 의존성 배열([])이 비어있으므로, 처음 만든 객체를 기억(Memoization)해서 계속 재사용함.
// -> 주소가 변하지 않으므로 하위 컴포넌트 리렌더링 방지.
const MyProvider = () => {
  const value = useMemo(() => ({ name: "User", age: 25 }), []);
  return <Context.Provider value={value}>...</Context.Provider>;
}
```

### 참고
<a href="https://github.com/softeerbootcamp-7th/Team7-Jackpot/wiki/%5B%EB%B0%95%EC%86%8C%EB%AF%BC%5D-%E2%80%90-%EC%83%81%ED%83%9C-%EA%B4%80%EB%A6%AC-%EC%95%84%ED%82%A4%ED%85%8D%EC%B2%98%EC%9D%98-%ED%95%B5%EC%8B%AC-%EC%9B%90%EB%A6%AC%EC%99%80-%EC%84%A4%EA%B3%84-%EC%B2%A0%ED%95%99">[박소민] ‐ 상태 관리 아키텍처의 핵심 원리와 설계 철학</a>