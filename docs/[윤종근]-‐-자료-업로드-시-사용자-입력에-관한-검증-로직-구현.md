# 📖 자료 업로드 시 사용자 입력에 관한 검증 로직 구현

## ✨ 한 줄 요약
> 사용자가 입력한 필드(기업명, 직무명, 제출일, 문항) 및 서버 라벨링 결과에 대한 검증 로직을 구현하여 불완전한 데이터 저장을 방지하고 필드 수준 오류를 시각적으로 표시

## 🎯 등장 배경
- 왜 이 개념/도구가 등장했는가? 사용자가 업로드한 PDF 파일로부터 추출/AI 라벨링한 자기소개서 데이터(기업명, 직무명, 문항, 답변)를 사용자가 수정하는 과정에서 필드가 빠지거나 불완전하게 저장되는 문제 방지
- 해결하고자 하는 문제:
  - 사용자가 필수 필드(기업명, 직무명, 제출일, 문항 유형, 문항 제목, 답변)를 입력하지 않은 채 저장 버튼을 누를 때의 무결성 모장
  - 제출일처럼 여러 입력 필드(연/월/일)로 구성된 복합 필드의 완성도 검증(나의 채용공고 기능에서 캘린더에 보여주기 위해 유효한 날짜가 저장되어야 함)
  - 각 필드별 상세한 오류 메시지로 사용자 경험 향상

## 🔑 핵심 개념
- `.trim()`을 활용한 필드 레벨 검증:
  - 모든 텍스트 입력에 `.trim()`을 적용하여 공백만 있는 입력을 빈 값으로 처리
- 복합 날짜 필드 검증:
  - 제출일은 연/월/일 세 개 입력 필드로 구성되고 YYYY-MM-DD 형식 문자열을 파싱하여 추출
  - 유효성 검증 함수로 윤년, 월별 최대일 등을 고려한 논리적 유효성 검사 (팀원이 만든 로직에 UX 향상을 위한 로직 및 UI 추가)
  - 연/월/일 중 하나라도 비어있으면 오류 처리
- 라벨링 데이터의 초기 실패 감지:
  - 서버에서 반환한 라벨링 결과에서 문항과 답변이 추출되지 않았는지 검사 (기획 상 문항과 답변이 모두 추출되지 않은 경우는 실패처리)
  - 그 외에는 UI로 사용자의 입력이 필요한 필드를 인식시켜 입력 유도
- 저장 시 통합 검증
  - 저장 버튼 클릭 시 모든 탭과 문항에 대해 필드를 순회하며 빈 값을 배열에 수집하고 해당 배열의 길이가 0보다 크면 모달로 비어있는 필드를 사용자에게 보여줌
- 문항 유형 변환
  - AI가 분류할 수 있는 라벨의 범위가 정해져 있으므로 미리 API 스펙에 맞는 영문으로 변환하고 혹시나 없다면 원본값을 유지하도록 안전장치 설계

## ⚙️ 동작 방식
1. 라벨링 결과 로드 (초기 오류 감지)
  - 서버에서 AI 라벨링 결과를 받음
  - 각 문항의 질문/답변 필드를 검사
    - 둘 다 비어있으면 실패 처리
    - 하나만 비어있으면 UI 상으로 사용자에게 추가 입력을 유도하여 마저 AI 라벨링을 할 수 있도록 유도
2. 사용자 입력 수정 (상태 관리)
  - 커스텀 훅으로 각 자기소개서별 입력값(기업명, 직무명, 채용 시기, 제출일, 문항 유형) 지정
  - 훅으로 문항/답변의 라벨링 성공 여부를 추적하여 수정 가능 여부를 결정
3. 저장 시 검증 (통합 검사)
  - 사용자가 저장하기 버튼을 누르면 빈 값을 배열에 수집
  - 수집한 배열의 길이가 0보다 크다면 미입력 필드가 있다는 것이므로 모달로 미입력 필드 정보를 보여주어 UX 향상과 무결성 보장
4. 데이터 변환 및 저장
  - 한글 문항 유형을 서버에서 사용하는 영문으로 변환
  - 제출일과 채용 시기 포맷팅
  - API 요청 형식에 맞게 데이터 조립 후 POST

## 👍 장점
- 포괄적인 검증: 필드 수준, 날짜 논리, 서버 라벨링 결과까지 모두 확인하여 불완전한 데이터 저장을 방지
- UX 향상: 사용자에게 누락된 항목을 모달로 보여주어 사용자 경험 향상

## 👎 단점
- 로직을 직접 구현해야하므로 라이브러리 사용을 고려해볼 수 있음

## 🧩 언제 사용하면 좋은가?
- 사용하기 좋은 상황:
  - form 필드가 많고 필수 항목이 여러 개인 경우
  - 복합 데이터(날짜 등)의 유효성 검사가 필요한 경우
  - 서버에서 자동 생성/추출한 데이터를 사용자가 수정해야하는 경우
  - 사용자에게 명확한 피드백을 제공해야하는 경우
- 피해야 할 상황:
  - 간단한 폼이나 실시간 유효성 검사가 필요한 경우
  - 서버가 완벽하게 검증을 하여 클라이언트에서 복잡한 검증 과정을 거칠 필요가 없는 경우